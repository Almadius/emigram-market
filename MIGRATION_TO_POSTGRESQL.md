# Миграция с SQLite на PostgreSQL

Этот документ описывает изменения, внесенные для миграции проекта с SQLite на PostgreSQL.

## ✅ Выполненные изменения

### 1. Конфигурация базы данных

**Файл: `config/database.php`**
- ✅ Изменено значение по умолчанию: `'default' => env('DB_CONNECTION', 'pgsql')`

**Файл: `.env`**
- ✅ Обновлено подключение к БД:
  ```env
  DB_CONNECTION=pgsql
  DB_HOST=postgres
  DB_PORT=5432
  DB_DATABASE=emigram_market
  DB_USERNAME=emigram
  DB_PASSWORD=emigram_password
  ```
- ✅ Обновлены драйверы кеша и очередей на Redis:
  ```env
  CACHE_STORE=redis
  SESSION_DRIVER=redis
  QUEUE_CONNECTION=redis
  REDIS_HOST=redis
  ```

**Файл: `.env.example`**
- ✅ Обновлен шаблон конфигурации с PostgreSQL настройками
- ✅ Добавлены комментарии для различных секций

### 2. Docker конфигурация

**Новые файлы:**
- ✅ `docker-compose.yml` - конфигурация всех сервисов (PostgreSQL, Redis, App, Nginx, Queue, Scheduler)
- ✅ `Dockerfile` - образ для Laravel приложения с PHP 8.2, PostgreSQL драйверами
- ✅ `docker-entrypoint.sh` - скрипт инициализации контейнера
- ✅ `docker/nginx/default.conf` - конфигурация Nginx
- ✅ `.dockerignore` - файлы, которые не нужны в Docker образе

### 3. CI/CD

**Новый файл: `.github/workflows/deploy.yml`**
- ✅ Автоматический деплой на продакшен при пуше в `main`
- ✅ Установка зависимостей (Composer, NPM)
- ✅ Сборка фронтенда
- ✅ Деплой через SCP
- ✅ Миграции и оптимизация кеша

### 4. Миграции

**Файл: `database/migrations/2025_12_31_073627_create_products_table.php`**
- ✅ Обновлен комментарий: удалено упоминание SQLite

Все остальные миграции уже совместимы с PostgreSQL (используют стандартные методы Laravel Schema Builder).

### 5. Документация

**Новые файлы:**
- ✅ `README.md` - полная документация проекта
- ✅ `MIGRATION_TO_POSTGRESQL.md` - этот документ

## 🚀 Запуск с PostgreSQL

### Локально с Docker

```bash
# 1. Клонировать репозиторий
git clone <repo> emigram-market
cd emigram-market

# 2. Скопировать .env
cp .env.example .env

# 3. Запустить контейнеры
docker compose up -d

# 4. Выполнить миграции
docker compose exec app php artisan migrate

# 5. Создать администратора Filament
docker compose exec app php artisan make:filament-user
```

### Production деплой

```bash
# 1. Настроить GitHub Secrets:
#    - SERVER_HOST
#    - SERVER_USER
#    - SSH_PRIVATE_KEY

# 2. Создать master .env на сервере
cp /home/deploy-user/emigram-market/.env.example /home/deploy-user/.env.market.master
nano /home/deploy-user/.env.market.master
# Настроить APP_ENV=production, APP_DEBUG=false, DB credentials

# 3. Пушить в main ветку
git push origin main
# GitHub Actions автоматически задеплоит
```

## 🔄 Порты

- **API/Admin:** 8002 (был 8000)
- **PostgreSQL:** 5433 (был 5432) - чтобы не конфликтовать с другими БД
- **Redis:** 6381 (был 6379) - чтобы не конфликтовать с другими Redis

## ⚠️ Важные замечания

1. **Данные не переносятся автоматически**
   - Если у вас были данные в SQLite, их нужно экспортировать и импортировать в PostgreSQL вручную
   - Или выполнить заново `php artisan db:seed`

2. **Все тесты нужно перезапустить**
   - SQLite и PostgreSQL могут иметь различия в поведении
   - Убедитесь, что все тесты проходят с PostgreSQL

3. **Производительность**
   - PostgreSQL быстрее SQLite для многопользовательских приложений
   - Redis кеширование значительно ускоряет работу

4. **Backup**
   - Настройте регулярные бэкапы PostgreSQL
   - Используйте `pg_dump` или специализированные инструменты

## 🔍 Проверка совместимости

Все миграции проверены на совместимость с PostgreSQL:
- ✅ Использование стандартных типов данных Laravel
- ✅ Нет SQLite-специфичных функций
- ✅ Foreign keys правильно настроены
- ✅ Индексы работают корректно

## 📞 Проблемы?

Если возникли проблемы:

1. Проверьте логи:
   ```bash
   docker compose logs app
   docker compose logs postgres
   ```

2. Проверьте подключение к БД:
   ```bash
   docker compose exec postgres pg_isready -U emigram
   ```

3. Очистите кеш:
   ```bash
   docker compose exec app php artisan optimize:clear
   ```

## ✨ Что дальше?

- [ ] Настроить автоматические бэкапы PostgreSQL
- [ ] Настроить мониторинг (Prometheus + Grafana)
- [ ] Оптимизировать индексы под реальную нагрузку
- [ ] Настроить репликацию PostgreSQL для высокой доступности (опционально)
